[data]
    name = "Jarvis Peng"
    email = "cpeng@roku.com"
    github_username = "jarvispengAI"

# Bitwarden integration — chezmoi will call `bw` CLI automatically
# Make sure `bw login` + `export BW_SESSION=$(bw unlock --raw)` before `chezmoi apply`
[bitwarden]
    command = "bw"

[git]
    autoCommit = true
    autoPush = false
