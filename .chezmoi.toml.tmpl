[data]
    name = "Jarvis Peng"
    email = "cpeng@roku.com"
    github_username = "jarvispengAI"

# 1Password integration — chezmoi will call `op` CLI automatically
# Make sure `op signin` has been run before `chezmoi apply`
[onepassword]
    command = "op"

[git]
    autoCommit = true
    autoPush = false
